Custom event loop based on `liburing` wrapper `one-ring-core` for classic coroutine based async programmming. IO-level coroutines register events with the event loop using typed yields.

To be updated with examples.
